version: 0.0
os: linux
files:
  - source: ./
    destination: /home/ubuntu/app
hooks:
  ValidateService:
    - location: scripts/docker.before.sh
      timeout: 3600
  # AfterInstall:
  #   - location: /home/ubuntu/app/docker.after.sh
  #     timeout: 180
