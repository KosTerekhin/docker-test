# sudo: required
# services:
#   - docker

# before_install:
#   - docker build -t kterekhin/react-solo -f Dockerfile.dev .

# script:
#   - docker run kterekhin/react-solo npm run test -- --watchAll=false

language: node_js
node_js:
  - "6"
services:
  - docker
cache:
  directories:
    - node_modules
deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    local_dir: dpl_cd_upload
    skip_cleanup: true
    on: &2
      repo: https://github.com/KosTerekhin/docker-test.git
    bucket: deploy-kterekhin
    region: eu-west-1

  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    bucket: deploy-kterekhin
    key: latest.zip
    bundle_type: zip
    application: KDeploy
    deployment_group: KDeploy
    region: eu-west-1
    on: *2
script:
  - zip -r latest *
  - mkdir -p dpl_cd_upload
  - mv latest.zip dpl_cd_upload/latest.zip
